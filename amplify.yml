version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g nodemon
        - npm install
    # IMPORTANT - Please verify your build commands
    build:
      commands: ["npm run start-1"]
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
